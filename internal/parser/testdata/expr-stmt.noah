a.b.c + b

1 + 2 / 3

1 * (2 - (3))

a = "2"

b == 3

a > 1

b < 2 + ~3

a && !b

c().d().e

a[1 + 1].b

a.b(1, c().d)

u.a = b = User.A{
    name: "foo",
    age: 123
}

a = fn() {
    return b;
}

a is str

a(fn(a: str, ...b: []bool) -> num {
    return -1 + 2.0;
})

b(P{ a: 1, b: "xx" })

let a: num = 'a' as num
let f: fn()
let f: fn(a: num, ...b: []any) -> str
let b: struct { a: num }
let c: struct <- a.B, struct{} { b: str; x: []any }
