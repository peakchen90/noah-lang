type TypeNum = num

pub type Vec = []char

pub struct Person {
    name: str
    age: num
}

enum Color {
    Red,
    Green,
}

interface Man {
    fn say(a: num) -> str,
    fn go(...args: []str);
}

interface Man2 <- Man {
}

pub interface Cindy {
    fn walk() -> [2]num;
}

struct Student <- Person.A: Man.B {
    grade: num
}

struct Student2 <- Student {
    grade: num
}